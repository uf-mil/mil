sProgramFinder:
Finds all files in a directory with either .py endings or #!/usr/bin/env in first line.
Then checks them for pep8 standards

Type in directory to search from as a second argument

This is how I run it:

rosrun mil_tools pythonFinder.py $HOME/mil_ws/src
